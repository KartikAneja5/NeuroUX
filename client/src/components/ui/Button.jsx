import { clsx } from 'clsx'; import { twMerge } from 'tailwind-merge'; export default function Button({ children, className, ...props }) { return <button className={twMerge(clsx('px-4 py-2 bg-primary-600 text-white rounded hover:bg-primary-700 transition', className))} {...props}>{children}</button>; }
