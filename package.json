{
  "name": "neuroux-marketplace-root",
  "version": "1.0.0",
  "description": "Unified launcher for NeuroUX UI/UX Component Marketplace",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "seed:db": "npm run seed --prefix server",
    "start:client": "npm run dev --prefix client",
    "start:server": "npm run dev --prefix server",
    "start:recommender": "python recommender/manage.py runserver 8000 --noreload",

    "dev": "npx concurrently \"npm run start:client\" \"npm run start:server\" \"npm run start:recommender\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
